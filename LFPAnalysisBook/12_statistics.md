# Statistics Utilities

The repository includes convenience utilities for permutation-based regression and time-resolved mixed-model workflows.

## Suggested use pattern

- validate the data structure first
- make the model formula explicit and version-controlled
- keep permutation counts small during smoke testing and large only for final analyses
- separate preprocessing QA from inferential statistics
